fastapi==0.109.2
uvicorn[standard]==0.27.1
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.1
pydantic==2.6.1
pydantic-settings==2.1.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 is not compatible with bcrypt>=5 during backend self-test.
bcrypt==4.0.1
python-multipart==0.0.9
celery[redis]==5.3.6
redis==5.0.1
aiofiles==23.2.1
# 20231117 fails to build with modern isolated setuptools (pkg_resources removed).
openai-whisper==20250625
moviepy==1.0.3
scenedetect[opencv]==0.6.2
# 23w46a is yanked/unavailable on modern PyPI indexes; keep a current stable release.
auto-editor==29.3.1
requests==2.31.0
httpx==0.26.0
python-dateutil==2.8.2
sentry-sdk[fastapi]==1.45.0
python-magic==0.4.27

# Auto Edit engine: PIL overlays/B-roll/popups + numpy SFX synthesis.
# (both are also pulled transitively by moviepy/whisper; pinned here to be
# explicit. numpy capped < 2.1 for numba compatibility.)
numpy>=1.24,<2.1
Pillow>=10.0

# Test dependencies used by backend/tests
pytest==9.0.2
pytest-asyncio==1.3.0
